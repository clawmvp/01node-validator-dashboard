import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/ThemeProvider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "01node Validator Dashboard | Profitability Analytics",
  description: "Analytics dashboard for 01node validator operations across 35+ blockchain networks. Track stake, APR, revenue, and profitability.",
  keywords: ["01node", "validator", "staking", "crypto", "cosmos", "solana", "sui", "analytics", "dashboard"],
  authors: [{ name: "01node" }],
  openGraph: {
    title: "01node Validator Dashboard",
    description: "Analytics dashboard for 01node validator operations",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen bg-background`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
